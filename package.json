{
  "name": "escrow-stack",
  "version": "1.0.0",
  "private": true,
  "description": "Stock Broker Client Dashboard — real-time web + mobile dashboard with live price updates over WebSockets.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n server,web -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "seed": "npm --prefix server run seed",
    "smoke": "npm --prefix server run smoke",
    "start:server": "npm --prefix server start",
    "start:web": "npm --prefix web run dev",
    "start:mobile": "npm --prefix mobile start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
